# IDX/BEI Quantitative Trading System — Dependencies
# Install order matters for GPU compatibility on Colab T4

# ── Core Data Science ──
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
scipy>=1.11
pyarrow>=12.0       # Parquet support
openpyxl>=3.1       # XLSX reading

# ── Deep Learning (PyTorch — pre-installed on Colab) ──
# torch>=2.0  (pre-installed on Colab with CUDA)
pytorch-tabnet>=4.0

# ── Gradient Boosting (GPU-enabled) ──
xgboost>=2.0
lightgbm>=4.0

# ── NLP / HuggingFace ──
transformers>=4.35
tokenizers>=0.15
sentencepiece>=0.1.99
accelerate>=0.24

# ── Visualization ──
matplotlib>=3.7
seaborn>=0.12
plotly>=5.17

# ── Utilities ──
tqdm>=4.66
python-dotenv>=1.0

# ── Install command for Google Colab ──
# Run this cell first in Colab:
# !pip install pytorch-tabnet xgboost lightgbm transformers accelerate openpyxl pyarrow --quiet
